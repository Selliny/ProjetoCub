version: '3.8'

services:
  mapa_app:
    build: .
    volumes:
      - .:/app
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      # Essa linha diz para o contêiner enviar a tela para o seu Windows
      - DISPLAY=host.docker.internal:0.0
    network_mode: host